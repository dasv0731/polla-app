{
  "name": "polla-scripts",
  "private": true,
  "type": "module",
  "description": "Scripts utilitarios fuera del bundle de la app (cleanup, smoke tests, etc).",
  "dependencies": {
    "@aws-sdk/client-cognito-identity-provider": "^3.660.0",
    "@aws-sdk/client-dynamodb": "^3.1041.0",
    "@aws-sdk/lib-dynamodb": "^3.1041.0"
  }
}
