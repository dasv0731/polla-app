// Test environment is initialized by @angular-builders/jest's own setup file
// (node_modules/@angular-builders/jest/dist/jest-config/setup.js), which calls
// `require('jest-preset-angular/setup-jest')` and runs `initTestEnvironment`.
// Adding another `initTestEnvironment` call here would throw
// "Cannot set base providers because it has already been called". Place any
// project-wide test setup (global mocks, custom matchers, ...) below.
export {};
